version: '3.8'

services:
  financial-crime-unit-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: financial-crime-unit-service
    ports:
      - "8087:8087"
    environment:
      - CONFIG_PATH=config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./certs:/etc/csic/certs:ro
    depends_on:
      - fcu-db
      - fcu-cache
    networks:
      - fcu-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8087/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  fcu-db:
    image: postgres:15-alpine
    container_name: fcu-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: csic_platform
    volumes:
      - fcu-db-data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    networks:
      - fcu-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  fcu-cache:
    image: redis:7-alpine
    container_name: fcu-cache
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - fcu-cache-data:/data
    ports:
      - "6379:6379"
    networks:
      - fcu-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  fcu-prometheus:
    image: prom/prometheus:v2.48.0
    container_name: fcu-prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    ports:
      - "9091:9090"
    networks:
      - fcu-network
    restart: unless-stopped

  fcu-grafana:
    image: grafana/grafana:10.0.0
    container_name: fcu-grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3001:3000"
    networks:
      - fcu-network
    restart: unless-stopped
    depends_on:
      - fcu-prometheus

networks:
  fcu-network:
    driver: bridge

volumes:
  fcu-db-data:
  fcu-cache-data:
  prometheus-data:
  grafana-data:
