apiVersion: v2
name: reporting
description: NEAM Platform Reporting Service - Report generation and scheduling
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: library
    version: "1.0.0"
    repository: "file://../library"
