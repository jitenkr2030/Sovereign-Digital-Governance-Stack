apiVersion: v2
name: policy
description: NEAM Platform Policy Service - Policy management and enforcement
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: library
    version: "1.0.0"
    repository: "file://../library"
